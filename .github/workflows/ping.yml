name: Keep Render Awake

on:
  schedule:
    - cron: "*/10 5-16 * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping site
        run: |
          curl -A "Mozilla/5.0 Chrome/130" -s https://rps-league-g275.onrender.com/health > /dev/null
